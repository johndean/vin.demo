/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standalone output → small, self-contained server for Railway.
  output: 'standalone',
  reactStrictMode: true,
};

export default nextConfig;
